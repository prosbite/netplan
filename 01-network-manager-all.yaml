network:
  version: 2
  renderer: networkd
  ethernets:
    enp0s3:  # Change to your interface name
      dhcp4: true
  # Add the following lines for a static IP configuration (optional):
  #   addresses: [192.168.1.100/24]
  #   gateway4: 192.168.1.1
  #   nameservers:
  #     addresses: [8.8.8.8, 8.8.4.4]